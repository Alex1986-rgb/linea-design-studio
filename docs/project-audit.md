{
  "summary": "Аудит всего проекта LINEA: движок, сайт, агенты, документы, демо-альбом — что есть и чего не хватает",
  "agentCount": 5,
  "logs": [],
  "result": {
    "sum": "# Аудит проекта LINEA — сводный отчёт\n\nДата среза: 02.08.2026, 01:28. Проверено чтением кода, `ls`/`grep` по факту и двумя прогонами движка (demo-brief.json → 82 файла; синтетический «визардный» бриф → 57 файлов).\n\n---\n\n## 1. Что есть\n\n### Слой 1. Движок альбома — `/Users/alexandr/projects/linea-design-studio/engine/generate.js` (1743 стр.) + `engine/presets.js` (175 стр.)\n- CLI без зависимостей: `node engine/generate.js <brief.json> [outdir]`, метры→мм, ~30 функций листов.\n- **12 поштучных листов на комнату**: обмерный план (цепочки, диагональ, S/P/h), демонтаж, монтаж ГКЛ (фальш-стены под ниши М1..Мn), 2 плана мебели, план пола (ёлка/плитка 600, стык на оси полотна, стрелка укладки), 4 развертки A/B/C/D, план потолка (2–3 уровня, LED, ниша штор), план электрики с высотами. Плюс 1 узел А (короб с LED, М 1:20).\n- **13 сводных поквартирных листов** (`drawFlat*`): обмер+экспликация, демонтаж, монтаж ГКЛ, планировочное решение с фото-врезками, экспликация+двери Д1–Дn, розетки, потолки, освещение, выключатели, схема включения групп, полы, отделка стен, тёплые полы. ГОСТ-подобный табличный штамп, легенды, нумерованные примечания.\n- Документы-генераторы: паспорт + пояснительная, ведомость чертежей (сквозная АИ-N), концепция, спецификация материалов с кодами FIN и SKU, смета HTML+CSV, `index.html`-просмотрщик, `manifest.json`.\n- Правила: `furnitureFor` (8 типов помещений), `lightsFor`, `nichesFor`, `bedWallFor`, `ceilingLevelsFor`, `electroFor`, `roomGeometry` (площади за вычетом проёмов).\n- `presets.js`: 6 стилей (палитра/отделки/полы/потолок/плинтус/двери/SKU/цена краски), автоподбор стиля регулярками, 3 тарифа, 8 расценок работ, 22 цены мебели и света.\n\n### Слой 2. Демо-альбом — `site/portfolio/demo/`\n- Ровно **74 SVG**: 01-kvartira 13, 01-obmer 15, 02-plany 10, 03-poly 5, 04-razvertki 20, 05-potolki 6, 09-elektrika 5. Заявленное в паспорте («74 листов») сходится с фактом по разделам один в один.\n- `vedomost.html`: сквозная АИ-1…АИ-74 без пропусков и дублей, привязка к разделу и масштабу + 5 строк документов.\n- `manifest.json`: 92 записи, битых ссылок нет.\n- Штамп на всех 74 листах. Экспликация помещений с площадями и спецификация дверей Д1–Д5 («проём 900×2050 · полотно 820×2000»).\n- 11 рендеров, все подключены в `koncept.html` и `index.html`; спецификация 15.9 КБ, смета HTML 18.5 КБ + CSV 9.6 КБ.\n- Канон 14 листов закрыт на 12/14 в 01-kvartira (+ сверхканонная схема включения освещения).\n\n### Слой 3. Сайт — `site/`\n- Всего **3 страницы**: `index.html`, `brief.html`, `portfolio/demo/index.html` (+ внутренние документы демо).\n- Лендинг: якоря `#process #kit #portfolio #price #faq #contacts`, 7 точек входа на бриф, canonical + OG + 2 JSON-LD (ProfessionalService с 3 офферами, FAQPage), инлайн-SVG-favicon. Цифры на лендинге (74 листа, 11 визуализаций, 13 сводных планов) совпадают с фактом в демо.\n- Визард: 6 шагов, 3 предзаполненные комнаты, 7 карточек стилей, drag&drop, наводящие вопросы.\n- `js/brief.js` собирает `brief.json` по схеме движка, даёт скачивание Blob, mailto и `localStorage`.\n- `css/main.css` (124 стр.): токены тёмной темы, общая система index+brief. `robots.txt` + `sitemap.xml` (3 URL).\n\n### Слой 4. Конвейер — `.claude/`, `docs/`, `tools/`, `examples/`\n- 8 агентов (7 стадий конвейера + `seo-marketer`), 3 команды (`new-client`, `regen`, `qa-project`), скилл `design-pipeline` с таблицей контрактов файлов.\n- `CLAUDE.md` + `README.md`, 8 документов в `docs/`, скрипты рендеров и PDF в `tools/`, эталонный `examples/demo-brief.json` (с `object.flat` и `rooms[].pos`).\n- Деплой фактически существует: ветка `gh-pages` в `origin` (Alex1986-rgb/linea-design-studio).\n\n---\n\n## 2. Несостыковки — заявлено, но не работает\n\n### 2.1. Смертельные для бизнеса (сайт)\n| # | Что заявлено | Что на самом деле |\n|---|---|---|\n| 1 | «Нажмите „Отправить“: конвейер стартует, папка проекта придёт на e-mail в течение 48 часов» (`brief.html:109`) | «Отправить» = `mailto:` (`brief.js:114-128`). Ни формы, ни бэкенда, ни webhook. Без почтового клиента mailto молча фейлится — **лид теряется целиком**. Никакой конвейер не стартует, письмо уходит *от* клиента. |\n| 2 | «✓ файл.pdf (240 КБ)» после загрузки обмеров | `takeFiles` (`brief.js:64-67`) сохраняет только имя и размер. Сам `File` никуда не уходит. |\n| 3 | Контакт студии `hello@linea.studio` (`index.html:156`, `brief.js:124`) | Домен проекту не принадлежит; сайт живёт на `alex1986-rgb.github.io`. Единственный канал связи — фейковый. |\n| 4 | Визуализации в базовом тарифе (hero `:48`, og `:11`, `#kit :85`, подпись демо `:95`) | В блоке цен визуализации — эксклюзив «Премиум» (`index.html:129`), при этом сам демо-проект тарифа «Бизнес» содержит 11 рендеров. Три взаимоисключающих утверждения на одной странице. |\n| 5 | Форма собирает имя, телефон, e-mail | Ни чекбокса согласия на обработку ПДн, ни политики конфиденциальности (grep по consent/policy/152-ФЗ = 0). |\n| 6 | Валидация брифа | `brief.js:33` — переход между шагами без проверок. Бриф проходится полностью пустым, размеры комнат подставляются дефолтами 3.5×3.0 (`brief.js:87-88`). |\n\n### 2.2. Разрыв «сайт ↔ движок» (главный системный дефект)\nВизард отдаёт комнаты только с `name/type/width/length` и `object` без `flat`. Движок строит `FLAT` только если `pos` есть у **всех** комнат (`generate.js:800-805`), иначе блок `if (FLAT)` (стр. 1647) **молча** пропускает все 13 сводных листов — без единой строки в stdout.\n\nПроверено прогоном: синтетический «как с сайта» бриф → папки `01-kvartira` нет, альбом **57 файлов вместо 74**. Теряются единственные в альбоме экспликация дверей, схема включения групп, тёплые полы, отделка стен. При этом двери подставляются по умолчанию (стена C, отступ 0.2 м) во все комнаты, окно — на стену A всех жилых. То есть **реальный клиентский бриф несовместим с тем каноном, который продают на лендинге**.\n\n### 2.3. Масштаб — заявлен неверно\n`flatStamp` печатает «М 1:100», `sheetOut` регистрирует `1:100` в ведомости, паспорт и просмотрщик пишут «М 1:100» — но `flatLayer` рисует тем же `px()` с `S=0.08`, что и поштучные листы, подписанные «М 1:50». **Оба масштаба не могут быть верны.** Плюс `pasport.html` противоречит сам себе: примечание «все чертежи в масштабе 1:50», а его же таблица состава для 01-kvartira — «М 1:100».\n\n### 2.4. Захардкоженная высота 2,7 м ломает любой другой потолок\nПри `object.ceilingHeight = 3.2` (проверено прогоном):\n- план потолка выдаёт «1 ур. +3,200 / 2 ур. +3,080»;\n- узел А на том же альбоме — «+2,700 / низ короба +2,580» (`mark(2700)/mark(2580)`, вылеты 450/100 — константы);\n- легенда `drawFlatCeiling` жёстко печатает «+2,580 / +2,700 / +2,460»;\n- `drawFlatObmer` печатает «H=2700 · наружные стены 200, перегородки 150» при любой реальной геометрии, `drawObmer` — «перегородки 150» текстом, хотя толщина перегородок из брифа нигде не берётся (`WT=12 px`, `EXT=200 мм` — константы). `object.flat{width,length,extWall,intWall}` из `demo-brief.json` движком **не читается вообще**.\n\n### 2.5. Геометрические допущения, которые молча ломают чертёж\n- **Окно = признак наружной стены.** `roomWalls` штрихует как наружные только стены с окнами; `flatLayer` всегда врезает окно в наружную полосу `EXT=200` (`fx(r.pos.x)−px(EXT)`). Окно на внутренней стене (бриф это разрешает) будет нарисовано **внутрь соседнего помещения**.\n- **Комнаты рисуются независимо**: проёмы на общей стене двух комнат нигде не сверяются — дверь может существовать в одной комнате и отсутствовать в смежной.\n- **Нет ни одной проверки**: перекрытие/зазоры `pos`-прямоугольников, ширина комнаты < 900 мм (короб 2×450 → отрицательные размеры rect), `offset+width` проёма > длины стены, комната без дверей. Ошибочный бриф молча выдаёт кривой SVG.\n- **Правила игнорируют размер помещения**: короб потолка 450 мм и «2 уровня» строятся даже в санузле 1,85×2,6 и в прихожей; сетка спотов с шагом 1150 даёт 20 точек в гостиной 6×5 без учёта мебели.\n\n### 2.6. Логические ошибки в спецификациях\n- `flatDoorList` помечает входной **любую** дверь помещения `type='hallway'`. Две двери в прихожей → две «Входные, сталь (сущ.)», обе исключаются из демонтажа.\n- Спецификация дверей печатает «проём W×H» из брифа рядом с полотном «W−80×**2000**» — высота полотна захардкожена и не связана с `o.h`.\n- Агрегация мебели в смете идёт по `key`: «Шкаф h2400» (спальня) и «Шкаф» (детская) сливаются в одну позицию с именем первой встреченной.\n- В смете 4 раздела и **ни одной** строки на демонтаж, гидроизоляцию, тёплый пол, двери, сантехнику, шторы, вывоз мусора, доставку, проектные работы — хотя листы демонтажа, тёплого пола и спецификация дверей в альбоме есть.\n\n### 2.7. Названия не совпадают между ведомостью, листом и документами\n| В ведомости | На самом листе | В паспорте/index |\n|---|---|---|\n| «План розеток и выводов» | «План электрики» | — |\n| «План расстановки мебели» | «Планировочное решение» | — |\n| «Монтаж перегородок. \\<комната\\>» | «План монтажа ГКЛ-конструкций» | «монтаж ГКЛ-конструкций под ниши» |\n\nПерегородки при этом явно не возводятся. В `02-plany` заявленная пара «с размерами / презентационный» **не различима и названа наоборот**: `plan-02-spalnya.svg` (без суффикса, зарегистрирован как «с размерами», АИ-29…33) — 5 размерных чисел; `plan-02-spalnya-mebel.svg` (суффикс `-mebel`, зарегистрирован как презентационный, АИ-34…38) — 0 размерных чисел.\n\n### 2.8. Единый альбом не собирается, штампы разные\n- Два разных штампа в одном альбоме: табличный ГОСТ-подобный на 13 листах `01-kvartira` против однострочного «Лист N из 74 · М 1:50 · стадия РП · дата» на остальных 61.\n- `tools/gen_pdf_album.sh` **не собирает PDF**: создаёт титул в `/tmp`, печатает подсказку про wkhtmltopdf/pdftk и удаляет временную папку. `SITE=\"$PROJ/site/portfolio/demo\"` при `OUT=\"$PROJ/album.pdf\"` — аргумент трактуется одновременно как проект и как репозиторий; в CSS строка `page-break-after: always;` находится вне какого-либо правила.\n\n### 2.9. Навигация и просмотрщик\n- `viewerHTML` генерирует `<section id=\"kvartira\">` с 13 сводными листами (самый ценный раздел), но **ссылки на неё в `<nav>` нет** — раздел недостижим из меню. В nav скачок 05 → 09.\n- Двойная нумерация в демо: «01 · Сводные планы квартиры» и следом «01 · Обмер, демонтаж, монтаж».\n- Фото-врезки в `drawFlatFurniture` читают миниатюры из `outDir/06-koncept/renders/thumbs` на этапе, когда рендеров ещё нет (`let renders = []` идёт **после** генерации листов). При первом прогоне href остаётся относительным и в `<img>` не грузится — это признано в комментарии самого кода и оставлено.\n- `06-koncept/renders/thumbs/` — 3 файла отсутствуют в `manifest.json` и не используются нигде: единственное расхождение диск↔manifest.\n\n### 2.10. Документация конвейера описывает несуществующий проект\n| Файл | Заявлено | Фактически |\n|---|---|---|\n| `chief-architect.md:12` | «00 паспорт, 01 планы, 02 развертки, 03 потолки, 04 концепция, 05 материалы, 06 смета» | 11 разделов, **ни один номер после 00 не совпадает**; 01-kvartira (13 листов), 03-poly, 09-elektrika вообще не входят в приёмку |\n| `estimator.md:10` | `06-smeta/`, `05-materialy/` | `08-smeta/`, `07-materialy/` |\n| `space-planner.md:22` | `01-plany/*.svg`, 1 план на комнату | `02-plany/`, 2 файла на комнату |\n| `visualizer.md:16-17` | `04-koncept/` | `06-koncept/` — расходится даже с собственными скриптами `tools/gen_renders*.sh` |\n| `visualizer.md:17` | «добавь ссылки в koncept.html» | `koncept.html` генерируется функцией `conceptHTML` и стирается при перегенерации — прямое нарушение правила CLAUDE.md «не править сгенерированное руками» |\n| `regen.md:6` | «планы = помещения, развертки = 4×, потолки = помещения» | на 5 комнат = 74 SVG (13+15+10+5+20+6+5). **Проверка по этой формуле не сойдётся никогда** |\n| `draftsman.md` | чек-лист на `drawPlan/drawElevation/drawCeiling/stamp/dim*` | в движке 18+ новых функций (все `drawFlat*`, `drawFloor`, `drawDemolition`, `drawMontage`, `drawNode`, `flatStamp`) без критериев приёмки |\n| `README.md` | дерево из 10 разделов, «демо 54 м²» | 11 разделов, нет 01-kvartira; `object.area = 56`, `manifest.totalArea = 56` |\n| `CLAUDE.md` | «docs/ — маркетинг- и SEO-планы», «7 агентов» | в docs/ ещё 6 исследований/аудитов; в `.claude/agents` 8 файлов |\n| `site/robots.txt:3` | `Disallow /portfolio/demo/06-smeta/smeta.csv` | такого пути нет (смета в `08-smeta/`) — правило мёртвое, CSV открыт. Дефект зафиксирован в `docs/page-audit.md:35` и **не исправлен** |\n| `SKILL.md` | контракт `clients/<slug>/project/**` | каталога `clients/` не существует и он в `.gitignore` — ни одного реального прогона конвейера в репозитории нет, отработан только `/regen` для демо |\n\nИз всех рекомендаций `docs/page-audit.md` (аудит от 01.08) выполнена **ровно одна**: убран жаргонный стат «1:50 рабочий масштаб» (`index.html:57`). Разделы 2/3/4 аудита не тронуты.\n\n### 2.11. Публикуется не то, что лежит локально\n`git status`: modified `engine/generate.js`, `site/index.html`, вся `site/portfolio/demo/`. На GitHub Pages опубликована предыдущая версия. Скрипта/воркфлоу деплоя в `gh-pages` нет ни в `tools/`, ни в `.github` (каталога `.github` нет вообще).\n\n### 2.12. Технический долг движка\n- **Дубли кода** один-в-один: группировка ниш (`drawMontage` ↔ `drawFlatMontage`), штриховка пола (`drawFloor` ↔ `drawFlatFloors`), символы розеток (`drawElectro` / `drawFlatElectro` / `drawFlatSwitches`), символы мебели (`furnSymbol` ↔ `drawFlatFurniture`).\n- **Мёртвый код**: `tot={spots,pendants,tracks}` в `drawFlatLighting` заполняется и не используется; в `CAD` не используются `newWall, plumb, ledDot, led, tp, tpCable, tpReg, finPaint, finTile, finApron`; ветка `f.key==='dining'` в `furnSymbol` недостижима; `FURN_PRICES.dresser` не используется; обращение к несуществующему `CAD.ink` (спасает `|| '#1C1C1C'`); `JSON.stringify([...] || '')` — `|| ''` мёртв.\n- **Пересчёт вместо кэша**: `lightsFor/electroFor/ceilingLevelsFor/nichesFor/furnitureFor` вызываются заново в каждом листе и в каждой строке легенды (в `drawFlatLighting` — 4 полных прохода по всем комнатам только ради подписей).\n- **Скрытая зависимость по состоянию**: `drawFlatMontage` передаёт данные в свою легенду через свойство функции `drawFlatMontage.marks`, полагаясь на порядок вызова `layerFn` перед `rightFn` внутри `flatSheet`.\n- **Две параллельные дизайн-системы**: демо использует inline-`<style>` с `#2b271f`/`#3a3428` и Georgia вместо токена `--line:#2E2921` и Playfair Display из `main.css`. Плюс два источника правды у hero: инлайн-градиент в `index.html:44` дублирует `.hero` из `main.css:32-34`, из-за чего LCP-картинку нельзя preload-нуть.\n\n---\n\n## 3. Чего не хватает\n\n### P0 — блокирует продажу или выпуск альбома клиенту\n\n| Слой | Пункт | Обоснование |\n|---|---|---|\n| Сайт | Реальный приём заявок (Web3Forms / Formspree / Telegram Bot API) | Единственный канал — `mailto:` в `brief.js:124`. Заявка физически никуда не уходит. |\n| Сайт | Загрузка файлов клиента на сервер | Обмеры и планы остаются в браузере, интерфейс при этом рапортует «✓ принято». |\n| Сайт | Валидация контактов + блокировка кнопки «Собрать бриф» | Пустой бриф проходится до конца, лид без контактов. |\n| Сайт | Чекбокс согласия ПДн + страница политики конфиденциальности | Собираются имя, телефон, e-mail без согласия. |\n| Сайт | Реальные контакты: телефон, Telegram/WhatsApp в шапке, `#contacts`, финале брифа | Сейчас — только несуществующий домен. |\n| Сайт | `demo/index.html`: title/description, canonical, OG, лого-ссылка на главную, CTA на бриф | Самая горячая страница-доказательство — тупик воронки, не индексируется и не шарится. |\n| Сайт | `@media ≤520px` для сеток `minmax(340px)/minmax(440px)` в демо | В инлайн-`<style>` демо ноль медиазапросов → на 375px горизонтальный скролл всей страницы. |\n| Сайт | Alt-тексты и человеческие подписи листов (через движок) | У 74 SVG `alt=\"\"`, `figcaption` = слаг файла; у всех 11 рендеров одинаковый alt. Картиночный трафик — главный SEO-актив ниши. |\n| Сайт | Оптимизация веса: `hero.jpg` 384 КБ без WebP/preload, 3.4 МБ рендеров грузятся полноразмерно и в превью, и по клику | LCP и Core Web Vitals на мобильных. |\n| Сайт+Движок+Конвейер | **Компоновка квартиры (`pos`/`object.flat`) в визарде + описание полей в `brief-analyst` и `SKILL.md`** | Без неё клиентский бриф теряет 13 из 74 листов молча. Единая корневая причина трёх дефектов. |\n| Движок | Валидация геометрии брифа (пересечения `pos`, окно на внутренней стене, ширина < 900 мм, `offset+width` > стены, комната без дверей) | Ошибочный бриф выдаёт кривой SVG без предупреждений. |\n| Движок | **Разрезы/сечения как класс** | В движке нет ни одной функции сечения. Для санузлов, ниш, коробов, порогов и стыков полов нет ни одного разреза. |\n| Движок | **Раздел ВК (водоснабжение и канализация)** | Нет плана разводки ХВС/ГВС/канализации, привязок приборов, ведомости сантехники. Трубы/инсталляция/смесители/гидроизоляция не считаются вовсе. В CAD даже зарезервирован цвет `plumb` — не используется. |\n| Движок | **Отопление** | Нет радиаторов, теплотрасс, привязок под окнами, теплопотерь. Тёплый пол — один лист-картинка и ноль строк в смете. |\n| Движок | **Кондиционирование и вентиляция** | Нет внутренних блоков, дренажа, трасс, вытяжки, приточки. При этом потолочные короба 450 мм проектируются вслепую, без резерва под трассы. |\n| Демо | Лист «Кондиционеры» (канон #14, частота 7/8) | Именно из-за него сводный раздел = 13 листов вместо канонических 14. |\n| Демо | Лист «Слаботочка» отдельно (канон #7, 4/8) | Есть только розетки 220В. |\n| Демо | Таблица «Условные обозначения» на каждом листе | Есть на 6 из 74 (все в 01-kvartira); в 01-obmer, 02-plany, 03-poly, 04-razvertki, 05-potolki, 09-elektrika — ноль. |\n| Демо | Блок «Примечания» внизу листа | 13/13 в 01-kvartira, 5/15 в 01-obmer, **0 из 46** в остальных разделах. |\n| Конвейер | Обновить чек-листы 7 агентов + SKILL.md + regen.md под реальные 11 разделов | Сейчас QA проверяет несуществующие папки — половина альбома выходит клиенту без проверки. |\n| Конвейер | CI: `.github/workflows` со smoke-прогоном движка + сверкой числа/имён листов с manifest | Каталога `.github` нет; движок правят 4 агента, регресс ловится только глазами. |\n\n### P1 — качество продукта и воронка\n\n| Слой | Пункт | Обоснование |\n|---|---|---|\n| Движок | Ведомость окон и оконных блоков (марки Оn, размеры, подоконники, откосы) | Ведомость дверей есть, окон — нет вообще. |\n| Движок | Библиотека узлов: примыкание пола к стене, теневой шов, порог санузла, скрытый плинтус, ниша штор, короб инсталляции, пирог пола | `drawNode` — единственный узел и полностью захардкожен. |\n| Движок | Слаботочка + однолинейная схема щита, группы, кабельный журнал | На листах декларируются «TV/LAN», «УЗО 30 мА», «мастер-выключатель», за которыми нет ни одного чертежа. |\n| Движок | Влияние `object.type` «дом/квартира» | Сейчас только печатается текстом. Для дома нет лестниц, этажности, наружных стен ≠200 мм, кровли/фундамента, котельной. |\n| Движок | Влияние `answers` на проект | При `wfh=«да»` кабинет получает 1 розетку и 1 выключатель (в `electroFor` нет `case 'cabinet'`); детская, питомцы, хранение не меняют мебель; dislikes не влияет на палитру. |\n| Движок | Пономерная ведомость отделки по ГОСТ (пол/плинтус/стены/потолок по каждому помещению, коды + площади) в машинночитаемом виде | Есть только сводная таблица и легенды. |\n| Движок | Деталировка мебели на заказ: развертки фасадов, планы с размерами, ведомость фурнитуры | Кухня/шкафы/ТВ-зона — один прямоугольник. |\n| Движок | Строки сметы: демонтаж, гидроизоляция, тёплый пол, двери, сантехника, шторы, вывоз мусора, доставка, проектные работы | Проверено по CSV: 4 раздела и ни одной из этих позиций при наличии соответствующих листов. |\n| Демо | Ключ-план и оси А/Б/В/Г на развертках | Оси латинские A/B/C/D, слова «ключ» нет — наша же заявленная отстройка не реализована. |\n| Демо | Реальная раскладка плитки (`<pattern>`, м² и штуки, стрелка начала укладки, подрезка, декоры) | Паспорт обещает раскладку в разделе 04, в файле — 0 `<pattern>` и текстовая строка «600×300, затирка в тон». |\n| Демо | Лист «Сантехприборы с привязками» | Отдельного сантехплана нет ни в одном разделе. |\n| Демо | Полноценная размерная цепочка на планах мебели | `plan-02-spalnya.svg` («с размерами») — 5 размерных чисел на комнату. |\n| Сайт | Убрать `<pre id=\"json\">` и золотую кнопку «Скачать brief.json» с финального экрана | Девелоперский артефакт в пик доверия клиента. |\n| Сайт | Проброс `brief.html?tariff=…` с кнопок «Выбрать» (`index.html:124,127,130`) | Выбор тарифа со страницы цен теряется. |\n| Сайт | Яндекс.Метрика + цели по шагам визарда | grep `ym(|gtag|metrika` = 0. Воронка неизмерима. |\n| Сайт | Автосохранение брифа в localStorage на каждом шаге | Сейчас `setItem` только в `buildResult` — закрытие вкладки на шаге 4 стирает всё. |\n| Сайт | OG + schema на `brief.html`, BreadcrumbList на демо | Правило CLAUDE.md «canonical+OG+schema на каждой странице» нарушено на 2 из 3. |\n| Сайт | FAQPage-схема на все 7 вопросов (в разметке 7 `<details>`, в JSON-LD 3); `@id/url/logo/email/unitText «м²»` в ProfessionalService | Частичный дубль и рассинхрон при правках; организация без url/logo не связывается со схемой. |\n| Сайт | `preconnect` к `fonts.gstatic.com` | Сейчас только `googleapis.com` — минус 100–300 мс на первом рендере. |\n| Сайт | Мобильное меню (burger) | В `main.css` ни одного `@media` для header/.nav/.links — 5 uppercase-ссылок съедают первый экран на 375px. |\n| Сайт | Секция-таблица «LINEA vs онлайн-планировщики» между `#portfolio` и `#price` | Главная отстройка спрятана в 7-м пункте FAQ. |\n| Сайт | Лайтбокс в демо (prev/next, Esc, «Лист N из 74») | 74 листа невозможно листать; `target=_blank` на сырой SVG даёт белую вкладку поверх тёмной темы. |\n| Сайт | Страницы `/ceny.html`, `/styles/`, контакты, шаблон кейса, `/404.html` | Коммерческие и среднечастотные запросы закрыты только якорями; 404 на Pages отдаётся дефолтный. |\n| Сайт | Демо на `main.css`/Playfair вместо инлайн-Georgia + пункт «Сводные планы» в nav | 13 сильнейших листов недостижимы из навигации; страница выглядит чужим сайтом. |\n| Конвейер | Рабочая сборка PDF-альбома вместо заглушки | «Полный комплект одним файлом» заявлен в позиционировании и недоступен. |\n| Конвейер | Скрипт/воркфлоу деплоя `site/` в `gh-pages` | Ветка обновляется руками, рабочее дерево уже разошлось с опубликованным. |\n| Конвейер | Агент/чек-лист сводных листов и ГОСТ-штампа, читающий `docs/cad-styleguide.md` и `docs/parity-report.md` | Стайлгайд и пофайловая сверка с эталоном существуют, но ни один агент на них не ссылается — знание не попадает в конвейер. |\n| Конвейер | Параметризовать `tools/gen_renders*.sh` (путь проекта, стиль, комнаты из brief.json) | Оба захардкожены на `site/portfolio/demo` и джапанди — для `clients/<slug>` визуализатор их запустить не может. |\n| Конвейер | Валидатор `brief.json` отдельным шагом перед движком | Сейчас `brief-analyst` валидирует «на глаз» по тексту инструкции. |\n\n### P2 — гигиена и рост\n\n| Слой | Пункт | Обоснование |\n|---|---|---|\n| Движок | Планы штор/карнизов, мокрые точки, план стяжки (пирог пола) | Упоминаются в примечаниях, отдельных листов и расчётов нет. |\n| Движок | Экспорт в PDF/DXF, форматы А3/А2 по ГОСТ 2.301/2.104 | Выход — SVG произвольного размера в px; штампы имитируют ГОСТ, но лист не привязан к формату бумаги. |\n| Демо | Единый PDF альбома; блок «до/после»; полный набор миниатюр (сейчас 3 из 11) | Канон P0 #1. |\n| Сайт | Возврат на шаг «Контакты» после сборки (`brief.js:28` прячет «Назад») | Опечатку в телефоне можно исправить только перезагрузкой с потерей всех ответов. |\n| Сайт | Пункт «Контакты» в nav; sticky-CTA на мобильном | Секция вне меню; 5 экранов без CTA под пальцем. |\n| Сайт | sitemap: `lastmod` вместо `priority/changefreq`, добавить документы демо; `og:url`, `twitter:card`, `apple-touch-icon` | Гигиена. |\n| Сайт | Пометки «необязательно» у 8 полей шага «Вопросы», убрать «Готово» из степпера | Снижает ощущаемую длину против обещанных 7 минут. |\n| Сайт | Блок доверия (имя/фото дизайнера, гарантия, оферта), «до/после» планировки, мини-калькулятор «площадь × тариф» | «Контроль дизайнера» упомянут трижды, но безличен. |\n| Конвейер | `package.json` со скриптами (`gen`, `gen:demo`, `qa`) + smoke-тест | Сырые пути `node engine/generate.js` дублируются в 5 местах документации. |\n| Конвейер | Распаковать 5 файлов `docs/` (сейчас JSON-дампы с экранированными `\\n`) + `docs/README` | `cad-styleguide.md`, `parity-report.md`, `research-*.md` не читаются ни человеком, ни агентом. |\n| Конвейер | Обновить `robots.txt` и `sitemap.xml` под структуру демо | Мёртвый Disallow, 3 URL без разделов альбома. |\n\n---\n\n## 4. Порядок работ\n\n### Волна 0 — «перестать терять деньги» (сегодня, ~4 часа)\n1. **Закоммитить и задеплоить текущее состояние.** На Pages сейчас старая версия; любые правки бессмысленно обсуждать, пока опубликованное ≠ локальному.\n2. **Приём заявок**: Web3Forms/Formspree на `brief.js` вместо `mailto` + загрузка файлов. Пока не сделано — весь трафик на сайт сжигается впустую.\n3. **Убрать ложное обещание** «конвейер стартует, папка придёт за 48 часов» (`brief.html:109`) до того, как конвейер действительно заработает. Это единственная формулировка, за которую придётся отвечать перед клиентом.\n4. **Согласие ПДн + страница политики** (юридический риск при живой форме).\n5. **Реальные контакты вместо `hello@linea.studio`** (`index.html:156`, `brief.js:124`) — почта на своём домене или Telegram.\n6. **Развести тарифы по визуализациям**: одно утверждение вместо трёх взаимоисключающих (`:11`, `:48`, `:85`, `:95`, `:129`).\n7. **Валидация контактов + блокировка «Собрать бриф»** при пустых полях.\n\n### Волна 1 — «замкнуть воронку» (1–2 дня)\n8. **`demo/index.html`**: head (title/description/canonical/OG), лого-ссылка на главную, CTA на бриф, `@media ≤520px`, пункт «Сводные планы» в nav, устранить двойную нумерацию «01 · … / 01 · …».\n9. **`viewerHTML` в движке** — те же правки в генераторе, иначе `/regen` их затрёт. Правило «не править сгенерированное руками» здесь обязательно.\n10. Alt-тексты и человеческие подписи листов в движке; сжатие hero и рендеров (WebP + srcset + preload).\n11. Метрика + цели по шагам; автосохранение брифа; проброс `?tariff=`.\n\n### Волна 2 — «починить корневой разрыв сайт↔движок» (2–3 дня)\n12. **Компоновка квартиры в визарде**: либо шаг раскладки `pos` (drag сетка), либо автораскладка из площадей + `object.flat`. Это разблокирует 13 из 74 листов для реальных клиентов.\n13. **Валидатор `brief.json`** отдельным модулем: схема + пересечения `pos` + окна на внутренних стенах + ширина < 900 мм + `offset+width` > стены + комната без дверей. **Плюс явный `console.warn`, когда `FLAT` не строится** — молчаливая потеря 13 листов недопустима.\n14. **Развязать константы высоты**: `mark(2700)/mark(2580)`, легенда `drawFlatCeiling`, `drawFlatObmer` («H=2700 · стены 200 · перегородки 150»), `EXT=200` → читать `object.ceilingHeight` и `object.flat.extWall/intWall`.\n15. **Определиться с масштабом сводных листов** и привести к одному значению `flatStamp`, `sheetOut`, паспорт, просмотрщик, `pasport.html`.\n16. Исправить `flatDoorList` (входная = только одна, по признаку в брифе), высоту полотна двери, агрегацию мебели в смете по `key+name`.\n\n### Волна 3 — «синхронизировать конвейер» (1 день, параллельно можно раньше)\n17. Обновить `chief-architect.md`, `estimator.md`, `space-planner.md`, `visualizer.md`, `draftsman.md`, `regen.md`, `SKILL.md`, `README.md`, `CLAUDE.md` под реальные 11 разделов и 74 листа. **Это дешевле всего и блокирует любую автоматическую приёмку.**\n18. **CI**: `.github/workflows` со smoke-прогоном демо-брифа + diff числа и имён листов против `manifest.json`. Без него волны 2 и 4 будут ломать альбом незаметно.\n19. Скрипт деплоя `site/` → `gh-pages`; `package.json` со скриптами.\n\n### Волна 4 — «добить канон альбома» (неделя+)\n20. Единый штамп на всех 74 листах + условные обозначения и примечания на каждом (закрывает 3 P0 демо разом, правится в одном месте — `sheetOut`/`flatSheet`).\n21. Кондиционирование (лист #14 канона) → альбом становится 14/14.\n22. Слаботочка отдельным листом + однолинейная схема щита.\n23. ВК и отопление как разделы + соответствующие строки сметы (демонтаж, гидроизоляция, тёплый пол, двери, сантехника, шторы, вывоз, доставка, проектные).\n24. Разрезы и библиотека узлов (параметризовать `drawNode`).\n25. Рабочая сборка PDF + формат листа по ГОСТ.\n\n### Волна 5 — рефакторинг и P2\n26. Вынести дубли (`nichesFor`-группировка, штриховка пола, символы розеток/мебели) в общие модули; вычистить мёртвый код; закэшировать `lightsFor/electroFor/ceilingLevelsFor` вместо 4 полных проходов на легенду; убрать `drawFlatMontage.marks` (скрытое состояние).\n27. Правила с оглядкой на размер помещения (короб 450 в санузле 1,85×2,6, споты без учёта зон).\n28. Влияние `answers` и `object.type` на проект. Ведомость окон. Деталировка мебели. Раскладка плитки.\n29. Единая дизайн-система (демо на `main.css`), доп. страницы `/ceny`, `/styles/`, `/404`, распаковка `docs/`.\n\n**Критический путь**: пункты 2 → 12 → 13 → 17 → 18. Всё остальное — улучшения поверх работающей системы; без этих пяти LINEA не может ни принять клиента, ни выдать ему полный альбом, ни заметить, что альбом сломался."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Аудит"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Сводка"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "audit:engine",
      "phaseIndex": 1,
      "phaseTitle": "Аудит",
      "agentId": "aebd03159b5fc3d72",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785659156810,
      "queuedAt": 1785659153312,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Движок генерации альбома: /Users/alexandr/projects/linea-de…",
      "promptPreview": "Аудит ДВИЖКА: /Users/alexandr/projects/linea-design-studio/engine/generate.js (1675 строк) и presets.js. Что генерируется, какие типы листов, какие данные брифа используются и какие ИГНОРИРУЮТСЯ (например: pos комнат, высоты, окна на разных стенах, тип объекта дом/квартира). Что в брифе можно указать, но движок не отрабатывает. Какие разделы рабочего альбома отсутствуют как класс (сантехника, отоп…",
      "lastProgressAt": 1785659508233,
      "tokens": 167629,
      "toolCalls": 27,
      "durationMs": 351422,
      "resultPreview": "{\"layer\":\"Движок генерации альбома: /Users/alexandr/projects/linea-design-studio/engine/generate.js (1743 стр., файл правится параллельно — снят на 01:28) + engine/presets.js (175 стр.). Проверено чтением кода и двумя прогонами: examples/demo-brief.json (82 файла) и синтетический бриф «как с сайта» (без pos/окон/дверей, тип «дом», h=3.2 м) — 57 файлов.\",\"have\":[\"Вход: brief.json (метры) → мм; CLI …"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "audit:album",
      "phaseIndex": 1,
      "phaseTitle": "Аудит",
      "agentId": "a02d931a35446d371",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785659159236,
      "queuedAt": 1785659153313,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Демо-альбом рабочей документации: /Users/alexandr/projects/…",
      "promptPreview": "Аудит ДЕМО-АЛЬБОМА: /Users/alexandr/projects/linea-design-studio/site/portfolio/demo/. Разделы 00–09 + 01-kvartira. Посчитай листы по разделам (ls), прочти index.html просмотрщика, 00-pasport/pasport.html и vedomost.html. Сходятся ли заявленные числа с фактическими файлами? Все ли разделы представлены в просмотрщике и ведомости? Чего не хватает в составе относительно канона рабочей документации (с…",
      "lastProgressAt": 1785659345973,
      "tokens": 84342,
      "toolCalls": 16,
      "durationMs": 186737,
      "resultPreview": "{\"layer\":\"Демо-альбом рабочей документации: /Users/alexandr/projects/linea-design-studio/site/portfolio/demo/\",\"have\":[\"Факт по ls: 01-kvartira 13 SVG, 01-obmer 15, 02-plany 10, 03-poly 5, 04-razvertki 20, 05-potolki 6, 09-elektrika 5 = ровно 74 чертёжных листа\",\"Заявленное сходится: pasport.html «Состав комплекта · 74 листов» и подсчёт по разделам (13/15/10/5/20/6/5) совпадают с файлами один в од…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "audit:site",
      "phaseIndex": 1,
      "phaseTitle": "Аудит",
      "agentId": "a3c05dfbee64dcd20",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785659158819,
      "queuedAt": 1785659153313,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Сайт LINEA (site/): index.html, brief.html, js/brief.js, cs…",
      "promptPreview": "Аудит САЙТА: /Users/alexandr/projects/linea-design-studio/site/index.html, brief.html, js/brief.js, css/main.css. Какие страницы есть и каких нет (цены, стили, кейсы, контакты, политика, 404). Работает ли приём заявок. Сверь docs/page-audit.md — что из тех рекомендаций уже сделано, а что нет (не повторяй сделанное).\nОтвечай строго по схеме: have (кратко), missing (с приоритетами), broken (несостык…",
      "lastProgressAt": 1785659308486,
      "tokens": 87843,
      "toolCalls": 13,
      "durationMs": 149666,
      "resultPreview": "{\"layer\":\"Сайт LINEA (site/): index.html, brief.html, js/brief.js, css/main.css + portfolio/demo/index.html; сверка с docs/page-audit.md (аудит от 01.08 15:59; index.html правился 02.08 01:00, demo перегенерён 02.08 01:19)\",\"have\":[\"Страниц всего 3: site/index.html (лендинг), site/brief.html (визард), site/portfolio/demo/index.html (просмотрщик демо-папки) + внутренние документы демо (00-pasport/p…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "audit:pipeline",
      "phaseIndex": 1,
      "phaseTitle": "Аудит",
      "agentId": "ad94d9e134a038959",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785659158255,
      "queuedAt": 1785659153313,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Конвейер и документы LINEA (.claude/agents · .claude/comman…",
      "promptPreview": "Аудит КОНВЕЙЕРА И ДОКУМЕНТОВ: /Users/alexandr/projects/linea-design-studio/.claude/agents/*.md, .claude/commands/*.md, .claude/skills/, CLAUDE.md, README.md, docs/*.md, tools/*.sh, examples/demo-brief.json. Соответствуют ли агенты и команды текущей структуре альбома (разделы переименовывались: было 01-plany/02-razvertki, стало 01-kvartira/01-obmer/02-plany/03-poly/04-razvertki/05-potolki/06-koncep…",
      "lastProgressAt": 1785659378442,
      "tokens": 77383,
      "toolCalls": 23,
      "durationMs": 220187,
      "resultPreview": "{\"layer\":\"Конвейер и документы LINEA (.claude/agents · .claude/commands · .claude/skills · CLAUDE.md · README.md · docs/ · tools/ · examples/)\",\"have\":[\"8 агентов в /Users/alexandr/projects/linea-design-studio/.claude/agents/: brief-analyst, concept-designer, space-planner, draftsman, estimator, visualizer, chief-architect (7 стадий конвейера) + seo-marketer (вне конвейера)\",\"3 команды в .claude/c…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "сводка",
      "phaseIndex": 2,
      "phaseTitle": "Сводка",
      "agentId": "a7c69d1b5c1175698",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785659513450,
      "queuedAt": 1785659511143,
      "attempt": 1,
      "promptPreview": "Сведи аудит проекта LINEA в отчёт на русском:\n[\n  {\n    \"layer\": \"Движок генерации альбома: /Users/alexandr/projects/linea-design-studio/engine/generate.js (1743 стр., файл правится параллельно — снят на 01:28) + engine/presets.js (175 стр.). Проверено чтением кода и двумя прогонами: examples/demo-brief.json (82 файла) и синтетический бриф «как с сайта» (без pos/окон/дверей, тип «дом», h=3.2 м) — …",
      "lastProgressAt": 1785659727609,
      "tokens": 59289,
      "toolCalls": 0,
      "durationMs": 214158,
      "resultPreview": "# Аудит проекта LINEA — сводный отчёт\n\nДата среза: 02.08.2026, 01:28. Проверено чтением кода, `ls`/`grep` по факту и двумя прогонами движка (demo-brief.json → 82 файла; синтетический «визардный» бриф → 57 файлов).\n\n---\n\n## 1. Что есть\n\n### Слой 1. Движок альбома — `/Users/alexandr/projects/linea-design-studio/engine/generate.js` (1743 стр.) + `engine/presets.js` (175 стр.)\n- CLI без зависимостей: …"
    }
  ],
  "totalTokens": 476486,
  "totalToolCalls": 79
}
---

## Статус исправлений (v5.0 → v5.3, 02.08.2026)

Исправлено движком:
- Все 74 листа переведены в CAD-стандарт эталона + фото готовых комнат на планах и развертках.
- Автокомпоновка квартиры при брифе без координат (было 57 файлов → стало 82, все 13 сводных листов).
- Единый масштаб 1:50; высоты и отметки уровней берутся из брифа (проверено на 3,2 м).
- Санитарные приборы расставляются с проверкой коллизий и зон открывания дверей.
- Ниши обходят проёмы своей и смежных комнат (глобальная проверка по координатам).
- Штамп авто-расширяется под текст; подписи проёмов, уровней потолка, ниш и электрики не перекрываются.

Осталось (требует решения оператора):
- P0 воронки: приём заявок (mailto → Telegram/Web3Forms), передача файлов клиента, реальные контакты, согласие ПДн, противоречие «визуализации в базе / только Премиум».
- P1 движка: валидация брифа, сверка проёмов между смежными комнатами, чтение object.flat, короб потолка в малых помещениях.
